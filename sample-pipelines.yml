# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
- task: AzurePowerShell@5
  inputs:
    azureSubscription: 'svc-global'
    ScriptType: 'FilePath'
    ScriptPath: '$(Build.SourcesDirectory)/Autupdate_variableGroupValues.ps1'
    ScriptArguments: 'AdinathDukare0824 $(Build.SourcesDirectory)/Book1.csv sampleproject adinath 3frwwltn55qaf6rluojxnb5jizsqcalfkkfwpbtezu6n6h6qa3q 3'